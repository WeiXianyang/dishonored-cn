# 游戏汉化最小修补工作流

> **通用版**：本文档是一个工具无关的 AI 工作流规范。复制全文粘贴给任意 AI 工具（ChatGPT、Claude、Reasonix、Cursor 等）即可使用，无需安装任何插件。

基于 Dishonored 天邈汉化 1.4p 项目的实战经验沉淀。适用于**基于现有汉化做最小修补**的场景——不改风格、只修硬错。

---

## 使用方式

**将本文档完整复制给 AI**，然后依次描述你的项目信息：

```
我正在修复 [游戏名] 的 [汉化组名] 汉化。
英文源目录：[路径]
中文源目录：[路径]
请按照本文档的工作流，从 Phase 0 开始。
```

AI 会按照 8 个阶段逐步引导你完成全部流程。每个阶段结束时，确认产出物再进入下一阶段。

---

## 适用场景

- 已有汉化质量不错但存在零星错译/漏译/术语不一致，需要系统性审校
- 源文本格式复杂（UE3 .int + UPK 字幕，或其他含标签/变量/换行签名的格式）
- 需要保留原汉化组的翻译底色，只做"修补"而非"重译"
- 需要可追溯、可审计的修改清单

## 不适用场景

- 从零开始的新翻译项目（没有基线译文可对照）
- 需要大范围改写的项目（这不是修补，是重译）
- 纯人工审校（AI 不可用的环境）

---

## 工作流概览（8 阶段）

```
Phase 0 环境准备 → Phase 1 双源提取对齐 → Phase 2 术语表建设
→ Phase 3 AI 校对流水线 → Phase 4 人工审核 → Phase 4.5 反方二审
→ Phase 5 合并生成 → Phase 6 打包 → Phase 7 验证
```

**3 次人工节点**：术语表确认（Phase 2）→ 不确定条目裁决（Phase 4）→ 游戏内实测（Phase 7）

---

## Phase 0：环境与仓库

**做什么**：初始化目录骨架、确认双源（英文原文目录 + 汉化中文目录）、确认模型后端。

**关键决策**：
- 模型后端选择（本地 CLI / API，是否支持 structured output 如 JSON Schema）
- 目录结构：`tools/` `data/` `glossary/` `prompt/` `patch/` `docs/`

**产出物**：git 仓库 + 目录骨架 + 模型调用链路验证

---

## Phase 1：双源提取与对齐

**做什么**：从英文源和中文源分别提取文本，按稳定身份对齐。

**关键步骤（按源类型）**：

### 游戏本地化文件（.int / .locres / .po 等）
- 解析对应的文件格式，提取文本叶子
- 稳定身份含：相对文件 + section + key + 出现序号 + 结构字段
- 保持原始编码和换行格式

### 内嵌字幕
- 英文：从游戏资源文件中提取对象属性
- 中文：解析字幕数据库（如 SQLite pickle、JSON 等）
- 哈希验证：英文内容哈希必须与字幕索引全量匹配

**对齐策略**：
- 按 文件+section+key+出现序号 对齐
- 每条记录附带 context（文件/关卡/说话人）和 domain（DLC/版本）
- 抽样验证对齐正确性

**⚠️ 教训**：务必验证**所有文件**都已纳入语料，不要遗漏子目录或非标准命名文件。实机验证时发现的遗漏很难回溯修复。

**产出物**：`data/aligned/corpus.jsonl`（双语对齐语料，每条含 id/en/cn/context/domain）

---

## Phase 2：术语表建设

**做什么**：从语料中提取专有名词候选，经 AI + 人工确认为术语锁。

**流程**：
1. 从语料中提取高频/高信息量的名词短语候选
2. AI 首审 → 冲突裁决 → **人工确认**（第一个关键人工节点）
3. 每个术语含：英文、批准中文、证据来源、频次、版本分布

### ⚠️ 关键教训——术语不是匹配上就套用

**决不要**把所有术语都做成全局字符串替换。这是本项目发现的最严重错误。

术语分为两类：

| 类型 | 含义 | 示例 |
|---|---|---|
| **硬锁** | 跨语境都正确的全局强制映射 | `Dunwall→顿沃`、`Outsider→界外魔` |
| **作用域候选** | 仅在限定场景生效，需结合语境判断 | `Regent's Safe→摄政王的保险箱` 仅限保险箱标签，不能污染 `Safe Room` |

**必须遵守的规则**：
- 短术语需复合词边界检查，避免子串匹配
- 跨 DLC/关卡的相同英文可能是不同实体（不同钥匙、不同地点）
- 术语命中后仍需 AI **独立复核**：普通所有格、UI标签、跨版本实体都可能否决术语表
- 术语表是参考而非命令——有语境证据时可以否决

**产出物**：`glossary/terms.json`（硬锁） + `glossary/advisory_terms.json`（作用域候选）

---

## Phase 3：AI 校对流水线（核心）

**做什么**：逐批让 AI 对照英文审核中文译文，标记需要修补的条目。

### 输入构造
每条提供给 AI 的信息：
- `id`、`en`（英文）、`cn`（旧译）
- `context`：文件/关卡/说话人/相邻台词
- `required_format`：标签、变量、换行签名（必须原样保留的不变量）
- `required_terms`：该条命中的硬锁术语及批准中文

### 分批策略
- 每批 30–40 条
- 按文件/关卡/对话路径聚合（保留语境）
- 每批独立落盘，支持断点续跑

### 模型配置
- 首轮使用中等推理强度（覆盖面优先）
- 疑难项使用高推理强度复审（uncertain + 低置信度 + 术语冲突）
- **必须使用 structured output**（JSON Schema），防止模型自由发挥
- 单并发，指数退避重试

### System Prompt 铁律

1. **旧译是基线，不是重译对象**——没有十足把握就不改（action=keep 是默认值）
2. 只修硬问题：错译、漏译、语义偏离、机翻痕迹、错别字、格式破坏
3. 专有名词以术语表为准（硬锁必须遵守，作用域候选可结合语境否决）
4. 格式签名必须保留（标签数量/顺序、变量名称/出现次数、换行签名）
5. 不确定就标记 `uncertain=true`，涉及世界观事实则请求查证
6. **等义就不改**——旧译已能正确传达语义时不得因"更顺口"而修改
7. **孤立短句不擅改时态与语气**——上下文不足以判断时不强行修改

### 质量控制
- 修改率红线：>35% 暂停排查
- 不确定率红线：>10% 暂停排查
- 占位符/标签/变量的硬违规必须为 0
- 每 500 条输出滚动统计
- 每批 id 全集校验：无遗漏、无重复、无多余

### 不确定处理流程

```
AI 标 uncertain=true
  → 编排层查询相关资料（Wiki / 官方文档 / 社区攻略）
  → 能裁决 → 直接 fix/keep
  → 不能裁决 → 保留给 Phase 4 人工审核
```

关键原则：资料搜索命中 ≠ 结论，需要直接支持当前事实。无结果不能当反证。不同来源冲突时降级为人工。

**产出物**：每批结果 + 汇总 accepted_fixes + uncertain 清单

---

## Phase 4：人工审核（占比极低）

**做什么**：对 AI 无法确定的条目（预期 <1%）进行上下文补齐后的人工裁决。

**上下文增强**：
- 补齐 DLC/任务/地点/触发类型
- 注入同场景对白邻居
- 注入查询结论
- 注入技术定位（文件/对话树路径）

### ⚠️ 教训——不要信任模型的自信度

本项目中从 145 条模型的"裁决"中拦截了 8 条过度/欠修。需要独立二次验收，逐项检查。

**产出物**：全部人工项裁决完毕（fix 或 keep），uncertain 归零

---

## Phase 4.5：反方二审（防过修稳定化）

### ⚠️ 这是从错误中学到的最重要机制

**为什么需要它**：本项目术语回溯中——1,207 条里 416 条被第二轮改变，187 条恢复旧译。同一个 AI 既能发现问题也能为修改背书，错误修改可能带着高置信度出现。

### 单写入规则

反方 AI 只能做三种裁决：
1. **接受候选**——确认旧译确有硬错且候选确实修复
2. **完整回退**到旧译——旧译可接受，或候选仅是润色
3. **请求研究/重新提案**——旧译和候选都不可靠

**绝对禁止写第三版译文**。

### 关键设计

- **隐藏首轮理由**——反方看不到 Phase 3/4 的理由和置信度，独立判断
- **风险分层**（决定处理顺序，不代替全覆盖二审）：
  - critical：否定/情态/数量/方向发生变化
  - high：大幅改写
  - medium：对白敏感改写
  - low：小改
- **错误疫苗**：
  - 保存已知典型错误作为测试用例
  - 每轮反方二审必须 100% 通过疫苗检测
  - 发现新错误类型必须加入疫苗库

### 证据优先级
1. 本条英文 + 本地同场景对白
2. 游戏脚本/实机证据
3. 官方中文资料/Wiki
4. 官方背景资料
5. 词典和语言直觉

### 发布硬门
- 所有语义修补具备独立裁决
- 错误疫苗 critical 检出率 100%
- 占位符/标签/变量/换行/编码错误 = 0
- 分层抽检错误修改率 <1%

**产出物**：最终 decisions（keep=保留候选，fix=回退旧译），uncertain 归零

---

## Phase 5：合并生成

**做什么**：将最终 decisions 写回实际文件。

**关键原则**：
- 本地化文件：保持原始编码 + 换行格式 + 键序不变（最小 diff）
- 字幕数据库：仅替换修改条目的值（最小 diff）
- 辅助文件原样复制（字体、索引、配置）

### ⚠️ 教训——changelog 过滤

- 变更清单必须过滤 `old == new` 的条目
- reason 与内容不一致的条目不应进入最终清单
- 每条修改都应对应一条可审计的记录

**产出物**：`patch/` 目录 + `changelog.json`

---

## Phase 6：打包

**双形态策略**：
- **Full**：全部文件解压覆盖即玩（大体积，体验好）
- **Lite**：安装脚本形态（小体积，需注入工具）

### ⚠️ 教训
- Full 和 Lite 的文件路径/注入顺序可能不同
- 字体和 UI 资源文件必须随包分发
- 脚本换行符必须匹配目标系统（Windows = CRLF）

**产出物**：`release/` 目录 + SHA-256 清单

---

## Phase 7：验证

**静态验证**（自动化）：
- 编码/结构/条目数校验
- 哈希校验（新包 vs 原版 vs 预期差异清单）
- 差异必须与预期一致，0 异常

**动态验证**（实机，最后一个关键人工节点）：
- 主菜单 → 进关卡 → 触发对话字幕 → UI/任务/物品/书页抽查
- ⚠️ 静态验证无法替代实机——字体遗漏、脚本错误只能实机发现

---

## 执行顺序

1. **Phase 0 → Phase 1**：确认双源，提取对齐，确认语料覆盖率
2. **Phase 2**：术语表建设，**必须人工确认**
3. **Phase 3**：先冒烟（2 批验证），再全量
4. **Phase 4**：人工审核不确定项
5. **Phase 4.5**：**不能跳过**——反方二审是防止过度修改的最后防线
6. **Phase 5 → Phase 6**：合并生成 + 打包
7. **Phase 7**：**实机验证不能省**——静态检查不了游戏内真实显示

每发现一种新错误类型 → 加入错误疫苗库。

---

## 文件结构参考

```
project/
├── tools/           # 提取、对齐、校对、打包脚本
├── data/
│   ├── aligned/     # corpus.jsonl（双语对齐语料）
│   └── review/      # AI 校对结果、人工裁决、反方二审
├── glossary/        # terms.json（硬锁）+ advisory_terms.json（作用域候选）
├── prompt/          # System prompt 模板
├── research/        # 查证记录、人工规则、错误疫苗库
├── docs/            # 各阶段报告
├── patch/           # 最终修补产物
└── release/         # 发布包
```

---

> 本工作流提炼自 [Dishonored 天邈汉化修补项目](https://github.com/WeiXianyang/dishonored-cn)，6,352 条修补、0 条人工悬而未决、实机验证通过。欢迎复用和反馈。
