# 通用游戏汉化安全修补工作流

> 面向“已有汉化的审校与修补”，不绑定《耻辱》、游戏引擎、文件格式、模型或 AI 平台。

这套工作流把现有汉化视为受保护的基线：**默认不改，只修能被原文、上下文或运行时证据证明的硬错**。所有修改都必须可追溯、可复核、可回退。

完整可复用 Skill：[`.reasonix/skills/localization-pipeline/SKILL.md`](.reasonix/skills/localization-pipeline/SKILL.md)

## 能用于哪些项目

- UE、Unity、Ren'Py、RPG Maker 或自研引擎；
- CSV/TSV、PO/XLIFF、JSON/YAML/XML、INI/properties、字符串表、SQLite/自定义数据库；
- UI、字幕、任务、物品、书信、系统文本；
- 本体、DLC、Mod 和跨版本修补；
- 官方汉化、社区汉化或个人汉化的最小修复。

它不承诺直接解包所有游戏资源。不同格式由“适配器”负责提取、稳定定位、写回、验证和打包；审校协议保持不变。

## 不适合的场景

- 没有基线译文的从零翻译；
- 以统一文风或大面积润色为目标的重译；
- 无法取得授权却计划重新分发受保护资源；
- 无法验证提取覆盖率、写回或回滚的项目。

若缺少源语言文本，只能处理错别字、格式、占位符等机械问题，不能声称完成了语义校对。

## 快速使用

把 Skill 目录复制到支持 `SKILL.md` 的 Agent 环境，或直接把 [`SKILL.md`](.reasonix/skills/localization-pipeline/SKILL.md) 提供给 AI，然后补充：

```text
我要修补：[游戏 / 版本 / 平台]
语言方向：[源语言] → [目标语言]
原文资源：[路径或获取方式]
现有汉化：[路径或获取方式]
覆盖范围：[本体 / DLC / Mod / 文本类型]
资源格式或引擎：[已知信息]
发布形态：[覆盖包 / 安装器 / Mod / 差分补丁]
请先完成 Phase 0，只在输入、身份和回滚方案确认后进入下一阶段。
```

## 通用架构：核心协议 + 格式适配器

格式适配器只做六件事：

1. `discover`：枚举所有范围内资源；
2. `extract`：提取原文、旧译和结构上下文；
3. `identify`：用结构而非文本本身生成稳定 ID；
4. `render`：仅把获准修改写入隔离区；
5. `verify`：重新提取并检查结构、编码、占位符和覆盖率；
6. `package`：生成可安装、可回滚的产物。

AI 审校只读取标准化语料，只输出按稳定 ID 绑定的提案和裁决。它不需要知道文本来自 `.po`、`.locres`、`.upk`、Unity 资产还是 SQLite。

适配器设计与往返测试见 [`references/adapters.md`](.reasonix/skills/localization-pipeline/references/adapters.md)。

## 8 阶段流程

```text
Phase 0 范围与冻结 → Phase 1 提取与对齐 → Phase 2 作用域术语
→ Phase 3 最小修补提案 → Phase 4 查证与人工裁决
→ Phase 5 独立反方二审 → Phase 6 确定性写回与打包
→ Phase 7 静态验证、实机验证与发布
```

### Phase 0：范围与冻结

确认游戏版本、平台、语言、DLC/Mod、资源格式、授权、安装与回滚方式。冻结原文和现有汉化哈希，在副本或 staging 中工作。

### Phase 1：提取与对齐

按资源路径、对象/section、字段/key、出现序号、版本等结构身份对齐双语文本。文本哈希只能用于漂移检测，不能单独充当 ID，否则重复短句会被错误合并。

保留重复文本、空覆盖、未匹配项、说话人、邻近对白、对象路径和格式签名。覆盖率未知时不得进入全量审校。

### Phase 2：作用域术语

术语分为全局稳定、大小写限定、UI/物品名限定、场景/说话人/版本限定，以及拒绝或歧义项。

最长匹配和词边界只能帮助召回，不能证明语义。短词污染复合词、跨 DLC 同名实体、普通所有格误判为专名，都是必须回归测试的风险。

### Phase 3：最小修补提案

`keep` 是默认值。只为错译、漏译、实体错误、关系颠倒、数量/否定/情态错误、错别字、占位符或格式损坏等硬问题生成候选。

旧译已等义时不得因“更顺口”修改。每个提案必须带稳定 ID、精确原文片段、旧译问题、最小改动和不确定问题。模型输出使用结构化 schema，ID 完整性和格式规则由代码校验。

### Phase 4：查证与人工裁决

每个疑难项只提出一个可证伪问题。证据优先级为：当前版本的本地资源与上下文 → 脚本/对象绑定/音频/实机截图 → 官方资料 → 可靠 Wiki 或攻略 → 词典。

搜索命中不等于证明，无结果也不等于反证。原始证据与翻译结论分开保存。

### Phase 5：独立反方二审

反方看不到首轮理由和置信度，只能：

- `accept_candidate`：接受候选；
- `revert_baseline`：完整回退旧译；
- `research_required`：隔离并继续查证。

**禁止反方写第三版译文。** 所有语义修改都要覆盖二审，风险分只决定先后顺序。

### Phase 6：确定性写回与打包

最终补丁是获准稳定 ID 的 allowlist。未解决项使用冻结旧译；未知、重复、缺失或冲突 ID 一律拒绝。占位符、标签、术语和结构从源数据重新计算，不能相信模型自报。

只向 staging 写入，重新提取结果，过滤 `old == new`，生成有效 changelog、哈希清单、安装与回滚说明。

### Phase 7：验证与发布

静态检查覆盖率、结构、编码、换行、标签、占位符、文件清单、预期 diff 和可复现构建。实机覆盖本体与所有范围内 DLC/Mod，抽测菜单、任务、字幕、笔记、物品、交互、选项、字体与回退路径。

发布包必须附带 manifest、有效 changelog、未解决/例外清单和已知边界。

## 三条最重要的安全线

1. **结构身份而非文本相等**：相同文本可能属于不同对象；
2. **反方只能接受、回退或请求研究**：防止二审继续“润色”；
3. **模型负责建议，代码负责放行**：覆盖率、ID、格式、写回和发布门必须确定性执行。

## 通用产物

```text
project/
├── adapters/          # 各引擎/格式提取、写回、验证与打包
├── data/aligned/      # 标准化双语语料
├── glossary/          # 作用域术语与拒绝项
├── review/            # 提案、反方裁决、人工队列
├── research/          # 证据记录与结论
├── regression/        # 金标准错误与变异测试
├── staging/           # 隔离写回区
└── release/           # 补丁、manifest、changelog、安装与回滚说明
```

数据契约与最小回归集见 [`references/contracts.md`](.reasonix/skills/localization-pipeline/references/contracts.md)。

## 已验证案例

这套方法最初在 [《耻辱 1》天邈汉化修补项目](https://github.com/WeiXianyang/dishonored-cn) 中形成：对齐 31,583 条语料，最终保留 6,352 处可审计修正，覆盖本体和全部 DLC，并通过独立反方二审、确定性写回与实机验证。

这些数字是案例结果，不是 Skill 的输入假设。其他游戏可以替换任意引擎、格式、知识源与模型，只要遵守同一组身份、证据、复核和发布契约。
