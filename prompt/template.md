# 术语表（英文原文 -> 天邈标准译名）

{terms}

# 待审核条目（JSON 数组，逐条审核）

{entries}

请按照系统指令逐条审核，输出 JSON 对象（`{"items": [...]}`）。
每条中只有 `required_terms` 是当前轮次的硬约束；若存在
`term_candidates`，它们是带作用域的参考译法或必须独立复核的
直接应用候选，不是必须采用或保留的结论。再次强调：保留天邈底色、只修硬问题、
不确定标 `uncertain`。
