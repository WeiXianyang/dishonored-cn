# 待审术语候选

下面 JSON 数组中的每项均来自 Phase 1 的真实中英语料：

{entries}

请严格按系统规则分类。特别注意：

- `seed_value` 只是待审旧种子，不是正确答案；若与证据冲突必须标出。
- `exact_cn_variants` 是英文标签与完整中文标签的直接配对，证据强，但残余引号、错字和多个变体仍需人工复核。
- `contexts` 只给有限例句；从例句提取的 `cn_term` 必须是其中中文的连续子串。
- 不要把整句、书信/任务标题、普通物件描述和句首大写词塞进术语表。

仅输出 `{"items":[...]}`。
